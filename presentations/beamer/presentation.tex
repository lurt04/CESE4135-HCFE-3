% TU Delft beamer template
% Author: Erwin Walraven (initial version was created by Maarten Abbink)
% Delft Universiy of Technology

%\documentclass{beamer}
% TU Delft beamer template
% Author: Erwin Walraven (initial version was created by Maarten Abbink)
% Delft Universiy of Technology

\documentclass{beamer}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta}
\usepackage[english]{babel}
\usepackage{calc}
\usepackage[absolute,overlay]{textpos}
\usepackage{graphicx}
\usepackage{subfig}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amsthm}
\usepackage{mathtools}
\usepackage{comment}
\usepackage{MnSymbol,wasysym}
\usepackage{tabularx}
\usepackage{multirow}

\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta}

\setbeamertemplate{navigation symbols}{} % remove navigation symbols
\mode<presentation>{\usetheme{tud}}

% ... rest of your preamble unchanged ...

% BIB SETTINGS
\usepackage[backend=bibtex,firstinits=true,maxnames=30,maxcitenames=1,mincitenames=1,url=false,style=authoryear]{biblatex}
\bibliography{bibfile}
\setlength\bibitemsep{0.3cm} % space between entries in the reference list
\renewcommand{\bibfont}{\normalfont\scriptsize}
\setbeamerfont{footnote}{size=\tiny}
\renewcommand{\cite}[1]{\footnote<.->[frame]{\citeauthor{#1} (\citeyear{#1})}}


\title[]{Home Care for the Elderly, week 2}
\institute[]{Delft University of Technology, The Netherlands}
\author{HCFE-3}
%\date{}

\begin{document}
{
\setbeamertemplate{footline}{\usebeamertemplate*{minimal footline}}
\frame{\titlepage}
}

{\setbeamertemplate{footline}{\usebeamertemplate*{minimal footline}}

}

\input{slides/background}


\begin{frame}{Problem statement}
\textit{\Large{``How can we unintrusively assist the elderly with mild cognitive decline, in staying at their homes for longer?''}}

\end{frame}

\input{slides/stakeholders}

\input{ObjectiveTreeGeneralOverview}

\input{ReduceFrictionObjectiveTree}

\begin{frame}{Cost minimization tree}
    \begin{block}{Stakeholders}
        \begin{itemize}
            \item Elderly and families - the system needs to be cheap to run and upkeep day to day\cite{ghosh2026}
            \item Health institutions - the system should reduce the costly consequences\cite{osterberg2005}
            \item Commercial companies - costs of the system should stay low
    \end{itemize}
\end{block}

    \centering
    \resizebox{0.7\linewidth}{!}{%
    \begin{tikzpicture}[
        root/.style={
            rectangle, rounded corners, draw=black, thick,
            minimum width=3.2cm, minimum height=1.2cm,
            align=center, font=\small\bfseries
        },
        box/.style={
            rectangle, rounded corners, draw=black, thick,
            minimum width=3.2cm, minimum height=1.2cm,
            align=center, font=\small
        },
        arrow/.style={-{Stealth[length=2.5mm]}, thick}
    ]

    % ROOT
    \node[root] (costs) {Minimization of costs};

    % LEVEL 1
    \node[box, right=2.0cm of costs, yshift=2.0cm] (impl)
        {Minimize\\implementation costs};
    \node[box, right=2.0cm of costs, yshift=0.0cm] (missed)
        {Minimize cost of\\missed doses};
    \node[box, right=2.0cm of costs, yshift=-2.0cm] (maint)
        {Minimize\\maintenance costs};

    % LEVEL 2
    \node[box, right=1.2cm of missed, yshift=1.0cm] (fatality)
        {Avoided\\fatality costs};
    \node[box, right=1.2cm of missed, yshift=-1.0cm] (hosp)
        {Avoided\\hospitalization costs};

    % CONNECTIONS
    \draw[arrow] (costs) -- (impl);
    \draw[arrow] (costs) -- (missed);
    \draw[arrow] (costs) -- (maint);
    \draw[arrow] (missed) -- (fatality);
    \draw[arrow] (missed) -- (hosp);

    \end{tikzpicture}
    }
\end{frame}



\input{LowMaintenance}

\input{slides/time_at_home}




\input{SystemDefinitionMatrix}

\end{document}
